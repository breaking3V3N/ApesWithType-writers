class SD:

    def __init__(self):
        self.Demand = 0
        self.Supply = 0

    def

"""
x_time = dict_iter(CountryDict[key], 0)
    y_value = dict_iter(CountryDict[key], 1)
    plt.plot(x_time, y_value, label=key + " (line) ")
    plt.plot(x_time, y_value, 'o', label=key + " (point) ")f
"""